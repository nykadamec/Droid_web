tunnel: webterm
credentials-file: /Users/nykadamec/.cloudflared/7c70058e-8f85-4e75-87ad-adb3f523f495.json

ingress:
  # HTTPS Frontend
  - hostname: webterm.nykadamec.dev
    service: https://localhost:3000
    originRequest:
      noTLSVerify: true
  
  # WebSocket Server
  - hostname: ws.webterm.nykadamec.dev
    service: http://localhost:8000
    originRequest:
      noTLSVerify: true
  
  # Catch-all rule (required)
  - service: http_status:404
